VITE_SUPABASE_URL=https://tu-proyecto.supabase.co
VITE_SUPABASE_ANON_KEY=tu-anon-key
# Para npm run migrate (opcional): Supabase → Project Settings → Database → Connection string (URI)
SUPABASE_DB_URL=postgresql://postgres.[ref]:[PASSWORD]@aws-0-[region].pooler.supabase.com:6543/postgres
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_xxx
VITE_APP_URL=http://localhost:5174
# Wallets maestras para depósitos USDC (las mismas que en Phantom/Edge Function)
VITE_MASTER_WALLET_SOLANA=
VITE_MASTER_WALLET_BASE=
VITE_MASTER_WALLET_POLYGON=
# Proxy: URL del server-proxy. El admin envía JWT (Authorization: Bearer); opcionalmente el proxy acepta DEPOSIT_EMAIL_SECRET en el body.
VITE_PROXY_URL=http://localhost:3031
# Server-proxy (send-deposit-email y send-retiro-procesado-email): Resend + Supabase service role para leer perfil. Admin usa JWT; no poner secretos en VITE_*
SUPABASE_SERVICE_ROLE_KEY=
RESEND_API_KEY=
RESEND_FROM=LA BOMBA <onboarding@resend.dev>
DEPOSIT_EMAIL_SECRET=

# ntfy.sh: aviso al admin (retiros/depósitos). Sin proxy: la página envía directo. Pon un tema secreto y suscríbete en https://ntfy.sh/app
VITE_NTFY_TOPIC=

# Telegram: aviso al admin SIN PROXY (la página envía directo al bot; el token queda visible en el cliente)
# Crear bot con @BotFather; chat_id: habla al bot y abre https://api.telegram.org/bot<TOKEN>/getUpdates
VITE_TELEGRAM_BOT_TOKEN=
VITE_TELEGRAM_ADMIN_CHAT_ID=

# Bot de usuarios: notificaciones al usuario (depósito acreditado, retiro procesado). Crear OTRO bot con @BotFather.
# El usuario vincula su cuenta desde la app (menú → Telegram notificaciones). Admin Phantom envía los mensajes con este token.
VITE_TELEGRAM_USER_BOT_TOKEN=
VITE_TELEGRAM_USER_BOT_USERNAME=

# IA para soporte y asistente admin: Groq (en línea, gratis, sin tarjeta). API key en https://console.groq.com
GROQ_API_KEY=
# Modelo (llama-3.1-8b-instant: 14.400 req/día free; llama-3.3-70b-versatile: 1.000 req/día)
GROQ_MODEL=llama-3.1-8b-instant
# Proxy: mismo bot/admin que arriba (VITE_*) para avisar al admin cuando el usuario pide contactar con el equipo
TELEGRAM_BOT_TOKEN=
TELEGRAM_ADMIN_CHAT_ID=
# Bot de usuarios: mensajes al usuario (depósito acreditado, retiro procesado). El proxy envía con este token (Render Web Service → TELEGRAM_USER_BOT_TOKEN).
TELEGRAM_USER_BOT_TOKEN=
# Sin webhook: mismo valor en Supabase app_config.telegram_webhook_secret. Los mensajes a Telegram llevan enlaces que al hacer clic ejecutan Aprobar/Rechazar.
TELEGRAM_WEBHOOK_SECRET=
# URL del proxy para enlaces Aprobar/Rechazar en Telegram. Debe ser accesible desde donde abras Telegram: en el móvil usa la IP de tu PC (ej: http://192.168.1.216:3031); en producción usa https://tu-dominio.com
ADMIN_LINKS_BASE=
